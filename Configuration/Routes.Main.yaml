-
  name: 'Login'
  uriPattern: 'mseid/login(/{base64EncodedCallbackUri})'
  defaults:
    '@controller': 'Authentication'
    '@action': 'login'
    'base64EncodedCallbackUri': ''
-
  name: 'Authenticate'
  uriPattern: 'mseid/auth'
  defaults:
    '@controller': 'Authentication'
    '@action': 'authenticate'
